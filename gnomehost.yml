---
# Useful for testing ..... 

- name: apply a minimal test configuration to the machine
  hosts: all
  remote_user: ansible
  become: true
 
  roles:
    - up2date_debian
    - setup
    - gnome
    - testmenu
    
